version: "3.9"

# DC2 (DR / Replica) stack
# Run: docker compose -f docker-compose.dc2.yml --env-file .env up -d

networks:
  dc-internal:
    driver: bridge

volumes:
  pg_data_dc2:
  pg_raft_dc2:
  minio_data_dc2:

services:

  # ── PostgreSQL + Patroni (built-in Raft DCS — no etcd needed) ─────────────
  postgres-patroni:
    build:
      context: ./patroni
      dockerfile: Dockerfile
    image: local/patroni:3.3.0
    container_name: postgres-dc2
    restart: unless-stopped
    networks: [dc-internal]
    ports:
      - "5432:5432"
      - "8008:8008"
      - "5010:5010"   # Patroni Raft peer port (must reach DC1)
    volumes:
      - pg_data_dc2:/home/postgres/data
      - pg_raft_dc2:/home/postgres/raft
      - ./patroni/patroni.dc2.yml:/etc/patroni/patroni.yml:ro
      - ./postgres/postgresql.conf:/etc/postgresql/postgresql.conf:ro
      - ./postgres/pg_hba.conf:/etc/postgresql/pg_hba.conf:ro
      - ./postgres/pgbackrest.conf:/etc/pgbackrest/pgbackrest.conf:ro
    environment:
      PATRONI_SUPERUSER_PASSWORD: ${PATRONI_SUPERUSER_PASSWORD}
      PATRONI_REPLICATION_PASSWORD: ${PATRONI_REPLICATION_PASSWORD}
      DC1_IP: ${DC1_IP}
      DC2_IP: ${DC2_IP}

  # ── MinIO ──────────────────────────────────────────────────────────────────
  minio:
    image: minio/minio:RELEASE.2025-05-24T17-08-30Z
    container_name: minio-dc2
    restart: unless-stopped
    networks: [dc-internal]
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data_dc2:/data
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_SITE_NAME: dc2
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
