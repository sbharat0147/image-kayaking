version: "3.9"

# DC1 (Primary) stack
# Run: docker compose -p dc1 -f docker-compose.dc1.yml --env-file .env up -d

networks:
  dc-internal:
    driver: bridge
  # Shared network so DC1, DC2, and witness can reach each other
  # by container name for Raft — avoids hairpin NAT via host IP.
  patroni-cluster:
    name: patroni-cluster
    driver: bridge
    ipam:
      config:
        - subnet: 172.30.0.0/24

volumes:
  pg_data_dc1:
  pg_raft_dc1:
  minio_data_dc1:
  prometheus_data:
  grafana_data:

services:

  # ── PostgreSQL + Patroni ──────────────────────────────────────────────────
  postgres-patroni:
    build:
      context: ./patroni
      dockerfile: Dockerfile
    image: local/patroni:3.3.0
    container_name: postgres-dc1
    restart: unless-stopped
    networks:
      dc-internal:
      patroni-cluster:
        ipv4_address: 172.30.0.10
    ports:
      - "5432:5432"
      - "8008:8008"   # Patroni REST API
      - "5010:5010"   # Raft peer port (exposed for external tooling)
    volumes:
      - pg_data_dc1:/home/postgres/data
      - pg_raft_dc1:/home/postgres/raft
      - ./patroni/patroni.dc1.yml:/etc/patroni/patroni.yml:ro
      - ./postgres/postgresql.conf:/etc/postgresql/postgresql.conf:ro
      - ./postgres/pg_hba.conf:/etc/postgresql/pg_hba.conf:ro
      - ./postgres/pgbackrest.conf:/etc/pgbackrest/pgbackrest.conf:ro
    environment:
      PATRONI_SUPERUSER_PASSWORD: ${PATRONI_SUPERUSER_PASSWORD}
      PATRONI_REPLICATION_PASSWORD: ${PATRONI_REPLICATION_PASSWORD}
      DC1_IP: ${DC1_IP}
      DC2_IP: ${DC2_IP}
      # Container IP on patroni-cluster network — used as connect_address
      # so other nodes can reach this node directly (not via 127.0.0.1).
      PATRONI_CONNECT_IP: 172.30.0.10

  # ── MinIO ──────────────────────────────────────────────────────────────────
  minio:
    image: minio/minio:RELEASE.2025-05-24T17-08-30Z
    container_name: minio-dc1
    restart: unless-stopped
    networks: [dc-internal]
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data_dc1:/data
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_SITE_NAME: dc1
      MINIO_PROMETHEUS_AUTH_TYPE: public
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ── Prometheus ────────────────────────────────────────────────────────────
  prometheus:
    image: prom/prometheus:v2.52.0
    container_name: prometheus
    restart: unless-stopped
    networks: [dc-internal]
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./monitoring/alerting-rules.yml:/etc/prometheus/alerting-rules.yml:ro
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.retention.time=30d"

  # ── Alertmanager ─────────────────────────────────────────────────────────
  alertmanager:
    image: prom/alertmanager:v0.27.0
    container_name: alertmanager
    restart: unless-stopped
    networks: [dc-internal]
    ports:
      - "9093:9093"
    volumes:
      - ./monitoring/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
    command:
      - "--config.file=/etc/alertmanager/alertmanager.yml"
      - "--storage.path=/alertmanager"

  # ── Grafana ───────────────────────────────────────────────────────────────
  grafana:
    image: grafana/grafana-oss:11.0.0
    container_name: grafana
    restart: unless-stopped
    networks: [dc-internal]
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana-provisioning:/etc/grafana/provisioning:ro
    environment:
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"

  # ── postgres_exporter ─────────────────────────────────────────────────────
  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.15.0
    container_name: postgres-exporter
    restart: unless-stopped
    networks: [dc-internal]
    ports:
      - "9187:9187"
    environment:
      DATA_SOURCE_NAME: "postgresql://postgres:${PATRONI_SUPERUSER_PASSWORD}@postgres-dc1:5432/postgres?sslmode=disable"
    depends_on:
      - postgres-patroni
